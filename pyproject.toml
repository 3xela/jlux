[project]
name = "jlux"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["equinox"]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
[tool.ruff.lint]
ignore = [
    "E501",
    "E731",
    "E741",
    "F821",   # jaxtyping uses string shape specs in annotations
]